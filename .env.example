# Configure these only in the future cloud API deployment environment.
# Never expose SMTP credentials to the desktop application.
SMTP_HOST=smtp.exmail.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USERNAME=lanmind@lansuan.cc
SMTP_PASSWORD=
SMTP_FROM=澜灵 <lanmind@lansuan.cc>
